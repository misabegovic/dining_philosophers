#include "caf/all.hpp"

using namespace caf;

namespace Configuration {

using take_atom = atom_constant<atom("take_atom")>;
using taken_atom = atom_constant<atom("taken_atom")>;
using eat_atom = atom_constant<atom("eat_atom")>;
using think_atom = atom_constant<atom("think_atom")>;
using put_atom = atom_constant<atom("put_atom")>;

} // Configuration